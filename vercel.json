{
  "framework": "nextjs",
  "buildCommand": "cd store && npm install --include=dev && npm run build",
  "outputDirectory": "store/.next",
  "installCommand": "cd store && npm install --include=dev"
}
